[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'node dist/database/migrate.js; node dist/database/seeds/run-seeds.js; exec node dist/main'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3